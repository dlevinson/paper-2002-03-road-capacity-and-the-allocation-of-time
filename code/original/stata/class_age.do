set matsize 800use ":Macintosh HD:Desktop Folder:paper:data1995.dta"drop if trvl_min>120drop if trpmiles>3699gen mode1=0gen mode2=0gen mode3=0gen mode4=0gen mode5=0gen mode6=0gen mode7=0gen tt1=0gen tt2=0gen tt3=0 gen tt4=0gen tt5=0gen tt6=0gen tt7=0gen trp1=0gen trp2=0gen trp3=0gen trp4=0gen trp5=0gen trp6=0gen trp7=0replace mode1=1 if mode==1replace mode2=1 if mode==2replace mode3=1 if mode==3replace mode4=1 if mode==4replace mode5=1 if mode==5replace mode6=1 if mode==6replace mode7=1 if mode==7replace tt1=trvl_min if mode==1replace tt2=trvl_min if mode==2replace tt3=trvl_min if mode==3replace tt4=trvl_min if mode==4replace tt5=trvl_min if mode==5replace tt6=trvl_min if mode==6replace tt7=trvl_min if mode==7replace trp1=trpmiles if mode==1replace trp2=trpmiles if mode==2replace trp3=trpmiles if mode==3replace trp4=trpmiles if mode==4replace trp5=trpmiles if mode==5replace trp6=trpmiles if mode==6replace trp7=trpmiles if mode==7gen hhpid= houseid*personid*1000drop if ds>4drop if ds==0collapse (sum) tt1 (sum) tt2 (sum) tt3 (sum) tt4 (sum) tt5 (sum) tt6 (sum) tt7 (sum) trp1 (sum) trp2 (sum) trp3 (sum) trp4 (sum) trp5 (sum) trp6 (sum) trp7 (mean) r_age r_sex driver (sum) mode1 (sum) mode2 (sum) mode3 (sum) mode4 (sum) mode5 (sum) mode6 (sum) mode7 worker, by (hhpid)  gen age1=0gen age2=0gen age3=0gen age4=0gen age5=0gen age6=0gen age7=0gen age8=0gen age9=0gen age10=0gen age11=0gen age12=0gen age13=0gen age14=0gen age15=0gen age16=0gen age17=0replace age1=1 if r_age>=5 & r_age<=9replace age2=1 if r_age>=10 & r_age<=14replace age3=1 if r_age>=15 & r_age<=19replace age4=1 if r_age>=20 & r_age<=24replace age5=1 if r_age>=25 & r_age<=29replace age6=1 if r_age>=30 & r_age<=34replace age7=1 if r_age>=35 & r_age<=39replace age8=1 if r_age>=40 & r_age<=44replace age9=1 if r_age>=45 & r_age<=49replace age10=1 if r_age>=50 & r_age<=54replace age11=1 if r_age>=55 & r_age<=59replace age12=1 if r_age>=60 & r_age<=64replace age13=1 if r_age>=65 & r_age<=69replace age14=1 if r_age>=70 & r_age<=74replace age15=1 if r_age>=75 & r_age<=79replace age16=1 if r_age>=80 & r_age<=84replace age17=1 if r_age>=85 & r_age<=89