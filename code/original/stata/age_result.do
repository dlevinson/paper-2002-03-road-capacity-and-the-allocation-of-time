use ":Macintosh HD:Desktop Folder:paper:age1995.dta"# Age (5-9)sum  mode1 trvl_min trpmiles  if mode1==0 & age1>0sum  mode2 trvl_min trpmiles  if mode2==0 & age1>0sum  mode3 trvl_min trpmiles  if mode3==0 & age1>0sum  mode4 trvl_min trpmiles  if mode4==0 & age1>0sum  mode5 trvl_min trpmiles  if mode5==0 & age1>0sum  mode6 trvl_min trpmiles  if mode6==0 & age1>0sum  mode7 trvl_min trpmiles  if mode7==0 & age1>0# Age (10-14)sum  mode1 trvl_min trpmiles  if mode1==0 & age2>0sum  mode2 trvl_min trpmiles  if mode2==0 & age2>0sum  mode3 trvl_min trpmiles  if mode3==0 & age2>0sum  mode4 trvl_min trpmiles  if mode4==0 & age2>0sum  mode5 trvl_min trpmiles  if mode5==0 & age2>0sum  mode6 trvl_min trpmiles  if mode6==0 & age2>0sum  mode7 trvl_min trpmiles  if mode7==0 & age2>0# Age (15-19)sum  mode1 trvl_min trpmiles  if mode1==0 & age3>0sum  mode2 trvl_min trpmiles  if mode2==0 & age3>0sum  mode3 trvl_min trpmiles  if mode3==0 & age3>0sum  mode4 trvl_min trpmiles  if mode4==0 & age3>0sum  mode5 trvl_min trpmiles  if mode5==0 & age3>0sum  mode6 trvl_min trpmiles  if mode6==0 & age3>0sum  mode7 trvl_min trpmiles  if mode7==0 & age3>0# Age (20-24)sum  mode1 trvl_min trpmiles  if mode1==0 & age4>0sum  mode2 trvl_min trpmiles  if mode2==0 & age4>0sum  mode3 trvl_min trpmiles  if mode3==0 & age4>0sum  mode4 trvl_min trpmiles  if mode4==0 & age4>0sum  mode5 trvl_min trpmiles  if mode5==0 & age4>0sum  mode6 trvl_min trpmiles  if mode6==0 & age4>0sum  mode7 trvl_min trpmiles  if mode7==0 & age4>0# Age (25-29)sum  mode1 trvl_min trpmiles  if mode1==0 & age5>0sum  mode2 trvl_min trpmiles  if mode2==0 & age5>0sum  mode3 trvl_min trpmiles  if mode3==0 & age5>0sum  mode4 trvl_min trpmiles  if mode4==0 & age5>0sum  mode5 trvl_min trpmiles  if mode5==0 & age5>0sum  mode6 trvl_min trpmiles  if mode6==0 & age5>0sum  mode7 trvl_min trpmiles  if mode7==0 & age5>0# Age (30-34)sum  mode1 trvl_min trpmiles  if mode1==0 & age6>0sum  mode2 trvl_min trpmiles  if mode2==0 & age6>0sum  mode3 trvl_min trpmiles  if mode3==0 & age6>0sum  mode4 trvl_min trpmiles  if mode4==0 & age6>0sum  mode5 trvl_min trpmiles  if mode5==0 & age6>0sum  mode6 trvl_min trpmiles  if mode6==0 & age6>0sum  mode7 trvl_min trpmiles  if mode7==0 & age6>0# Age (35-39)sum  mode1 trvl_min trpmiles  if mode1==0 & age7>0sum  mode2 trvl_min trpmiles  if mode2==0 & age7>0sum  mode3 trvl_min trpmiles  if mode3==0 & age7>0sum  mode4 trvl_min trpmiles  if mode4==0 & age7>0sum  mode5 trvl_min trpmiles  if mode5==0 & age7>0sum  mode6 trvl_min trpmiles  if mode6==0 & age7>0sum  mode7 trvl_min trpmiles  if mode7==0 & age7>0# Age (40-44)sum  mode1 trvl_min trpmiles  if mode1==0 & age8>0sum  mode2 trvl_min trpmiles  if mode2==0 & age8>0sum  mode3 trvl_min trpmiles  if mode3==0 & age8>0sum  mode4 trvl_min trpmiles  if mode4==0 & age8>0sum  mode5 trvl_min trpmiles  if mode5==0 & age8>0sum  mode6 trvl_min trpmiles  if mode6==0 & age8>0sum  mode7 trvl_min trpmiles  if mode7==0 & age8>0# Age (45-49)sum  mode1 trvl_min trpmiles  if mode1==0 & age9>0sum  mode2 trvl_min trpmiles  if mode2==0 & age9>0sum  mode3 trvl_min trpmiles  if mode3==0 & age9>0sum  mode4 trvl_min trpmiles  if mode4==0 & age9>0sum  mode5 trvl_min trpmiles  if mode5==0 & age9>0sum  mode6 trvl_min trpmiles  if mode6==0 & age9>0sum  mode7 trvl_min trpmiles  if mode7==0 & age9>0# Age (50-54)sum  mode1 trvl_min trpmiles  if mode1==0 & age10>0sum  mode2 trvl_min trpmiles  if mode2==0 & age10>0sum  mode3 trvl_min trpmiles  if mode3==0 & age10>0sum  mode4 trvl_min trpmiles  if mode4==0 & age10>0sum  mode5 trvl_min trpmiles  if mode5==0 & age10>0sum  mode6 trvl_min trpmiles  if mode6==0 & age10>0sum  mode7 trvl_min trpmiles  if mode7==0 & age10>0# Age (55-59)sum  mode1 trvl_min trpmiles  if mode1==0 & age11>0sum  mode2 trvl_min trpmiles  if mode2==0 & age11>0sum  mode3 trvl_min trpmiles  if mode3==0 & age11>0sum  mode4 trvl_min trpmiles  if mode4==0 & age11>0sum  mode5 trvl_min trpmiles  if mode5==0 & age11>0sum  mode6 trvl_min trpmiles  if mode6==0 & age11>0sum  mode7 trvl_min trpmiles  if mode7==0 & age11>0# Age (60-64)sum  mode1 trvl_min trpmiles  if mode1==0 & age12>0sum  mode2 trvl_min trpmiles  if mode2==0 & age12>0sum  mode3 trvl_min trpmiles  if mode3==0 & age12>0sum  mode4 trvl_min trpmiles  if mode4==0 & age12>0sum  mode5 trvl_min trpmiles  if mode5==0 & age12>0sum  mode6 trvl_min trpmiles  if mode6==0 & age12>0sum  mode7 trvl_min trpmiles  if mode7==0 & age12>0# Age (65-69)sum  mode1 trvl_min trpmiles  if mode1==0 & age13>0sum  mode2 trvl_min trpmiles  if mode2==0 & age13>0sum  mode3 trvl_min trpmiles  if mode3==0 & age13>0sum  mode4 trvl_min trpmiles  if mode4==0 & age13>0sum  mode5 trvl_min trpmiles  if mode5==0 & age13>0sum  mode6 trvl_min trpmiles  if mode6==0 & age13>0sum  mode7 trvl_min trpmiles  if mode7==0 & age13>0# Age (70-74)sum  mode1 trvl_min trpmiles  if mode1==0 & age14>0sum  mode2 trvl_min trpmiles  if mode2==0 & age14>0sum  mode3 trvl_min trpmiles  if mode3==0 & age14>0sum  mode4 trvl_min trpmiles  if mode4==0 & age14>0sum  mode5 trvl_min trpmiles  if mode5==0 & age14>0sum  mode6 trvl_min trpmiles  if mode6==0 & age14>0sum  mode7 trvl_min trpmiles  if mode7==0 & age14>0# Age (75-79)sum  mode1 trvl_min trpmiles  if mode1==0 & age15>0sum  mode2 trvl_min trpmiles  if mode2==0 & age15>0sum  mode3 trvl_min trpmiles  if mode3==0 & age15>0sum  mode4 trvl_min trpmiles  if mode4==0 & age15>0sum  mode5 trvl_min trpmiles  if mode5==0 & age15>0sum  mode6 trvl_min trpmiles  if mode6==0 & age15>0sum  mode7 trvl_min trpmiles  if mode7==0 & age15>0# Age (80-84)sum  mode1 trvl_min trpmiles  if mode1==0 & age16>0sum  mode2 trvl_min trpmiles  if mode2==0 & age16>0sum  mode3 trvl_min trpmiles  if mode3==0 & age16>0sum  mode4 trvl_min trpmiles  if mode4==0 & age16>0sum  mode5 trvl_min trpmiles  if mode5==0 & age16>0sum  mode6 trvl_min trpmiles  if mode6==0 & age16>0sum  mode7 trvl_min trpmiles  if mode7==0 & age16>0# Age (85-89)sum  mode1 trvl_min trpmiles  if mode1==0 & age17>0sum  mode2 trvl_min trpmiles  if mode2==0 & age17>0sum  mode3 trvl_min trpmiles  if mode3==0 & age17>0sum  mode4 trvl_min trpmiles  if mode4==0 & age17>0sum  mode5 trvl_min trpmiles  if mode5==0 & age17>0sum  mode6 trvl_min trpmiles  if mode6==0 & age17>0sum  mode7 trvl_min trpmiles  if mode7==0 & age17>0