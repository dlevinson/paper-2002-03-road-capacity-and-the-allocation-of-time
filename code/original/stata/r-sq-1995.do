use ":Macintosh HD:Desktop Folder:Paper:data95.dta"drop if ds>5drop if trpmiles>3699#drop if educ >32#drop if hhfaminc>97#drop if urban>1drop if trvl_min>120gen f1=0gen f2=0gen f3=0gen f4=0gen tt1=0gen tt2=0gen tt3=0gen tt4=0gen trp1=0gen trp2=0gen trp3=0gen trp4=0gen act1=0gen act2=0gen act3=0gen act4=0#drop if travday==1#drop if travday==7drop if r_age>65drop if r_age<18replace f1=1 if ds==1replace f2=1 if ds==2replace f3=1 if ds==3replace f4=1 if ds==4replace tt1=trvl_min if ds==1replace tt2=trvl_min if ds==2replace tt3=trvl_min if ds==3 replace tt4=trvl_min if ds==4replace trp1=trpmiles if ds==1replace trp2=trpmiles if ds==2replace trp3=trpmiles if ds==3 replace trp4=trpmiles if ds==4replace act1=act95 if ds==1replace act2=act95 if ds==2replace act3=act95 if ds==3replace act4=act95 if ds==4collapse (sum) f1 (sum) f2 (sum) f3 (sum) f4 (sum) tt1 (sum) tt2 (sum) tt3 (sum) tt4 (sum) trp1 (sum) trp2 (sum) trp3 (sum) trp4 (sum) act1 (sum) act2 (sum) act3 (sum) act4 (mean) houseid (mean) personid (mean) r_age (mean) r_sex (mean) hhfaminc (mean) worker (mean) hbppopdn (mean) stateid (mean) lif_cyc (mean) travday (mean) wrkcount (mean) hbhresdn (mean) drvrcnt (mean) tday_mon (mean) trpnum (mean) ref_educ (mean) ercode95, by (hhpid)gen sum1=act1+act2+act3+act4+tt1+tt2+tt3+tt4drop if sum1>1440drop if sum1<1440drop if ercode95>0drop if act1<0drop if act2<0 drop if act3<0 drop if act4<0gen tt95=tt1+tt2+tt3+tt4